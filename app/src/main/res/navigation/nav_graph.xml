<?xml version="1.0" encoding="utf-8"?>
<navigation
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools" android:id="@+id/nav_graph"
        app:startDestination="@id/listFragment">

    <fragment
            android:id="@+id/listFragment"
            android:name="my.test.kotlin_coroutines_hw.ui.LaunchListFragment"
            android:label="list_fragment"
            tools:layout="@layout/launch_list_fragment">

        <action
                android:id="@+id/action_listFragment_to_itemFragment"
                app:destination="@id/itemFragment"/>

    </fragment>


    <fragment
            android:id="@+id/itemFragment"
            android:name="my.test.kotlin_coroutines_hw.ui.LaunchFragment"
            android:label="item_fragment"
            tools:layout="@layout/launch_fragment">
    </fragment>

</navigation>